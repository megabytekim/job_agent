# Cloud Build pipeline for job-agent → Cloud Run
# Builds the Docker image from job-agent/Dockerfile, pushes to Artifact Registry,
# and deploys to Cloud Run. Designed for GitHub-triggered builds.

substitutions:
  _REGION: us-central1
  _SERVICE: job-agent
  _REPO: us-central1-docker.pkg.dev/$PROJECT_ID/containers
  _IMAGE: $_REPO/$_SERVICE:$COMMIT_SHA

steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-t", "$_IMAGE", "-f", "job-agent/Dockerfile", "job-agent"]

  - name: gcr.io/cloud-builders/docker
    args: ["push", "$_IMAGE"]

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - $_SERVICE
      - --image=$_IMAGE
      - --region=$_REGION
      - --platform=managed
      - --allow-unauthenticated
      - --set-env-vars
      - GOOGLE_CLOUD_PROJECT=$PROJECT_ID,GOOGLE_CLOUD_LOCATION=$_REGION

images:
  - $_IMAGE

options:
  logging: CLOUD_LOGGING_ONLY
